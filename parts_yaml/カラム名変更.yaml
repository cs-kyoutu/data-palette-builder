operation: カラム名の変更
format_single: |
  『カラム名の変更』
  「{old_name}」を""{new_name}""に変更する
format_multi: |
  『カラム名の変更』
  {renames_text}
variables:
  renames:
    type: list
    item:
      from: { type: text }
      to: { type: text }
